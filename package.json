{
  "name": "univ4-locker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ univ4-locker",
    "create-local": "graph create --node http://localhost:8020/ univ4-locker",
    "remove-local": "graph remove --node http://localhost:8020/ univ4-locker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 univ4-locker",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.78.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
